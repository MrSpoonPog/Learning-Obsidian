---
description: Cross-topic consolidation exam across everything solid
---

Run a cross-topic consolidation exam. Follow the `exam` skill (`.claude/skills/exam/SKILL.md`) in full.

Pull candidates from **every** topic in `knowledge/`, not just what's due — 10–15 items, structured in topic blocks (never interleaved across unrelated topics), drawn mainly from `[x]` solid nodes plus any aging `[~]` nodes.

If there isn't enough solid material yet across topics (e.g. only one topic exists and it's brand new), say so and suggest `/review` instead once more has landed.
